export type AuthUser = {
  id: string;
  email: string;
};

export type AuthData = {
  email: string;
  password: string;
};

export type SignUpCredentials = {
  id: string;
  email: string;
  password: string;
};
